# --- Dependencies / build artifacts ----------------------------------------
node_modules/
dist/
out/
*.vsix

# C++ CLI build output
cli-cpp/build/
cli-cpp/cmake-build/
cli-cpp/Testing/
*.exe
*.obj

# Transient / local state & evidence (environment-specific)
.b-evid*/
.bridge-e2e-evidence*/
.project-os-cli/
scratch/

# Artifacts: version the CLI evidence pack; keep environment-specific run data out.
artifacts/*
!artifacts/cli-v3/
!artifacts/cli-v3/**
artifacts/cli-v3/diagnostics/

# Env / secrets (never commit real secrets)
.env
.env.*
!.env.example
*.pem
id_rsa*
credentials*
*.key

# Logs / temp
*.log
*.tmp
*.zip
_CPack_Packages/
menu_out.txt

# OS
.DS_Store
Thumbs.db

# VS Code workspace settings (machine-specific paths)
.vscode/

# Project OS CLI runtime caches (published artifacts + provenance + shared bundles)
artifacts/published/
artifacts/provenance.json
artifacts/shared/


